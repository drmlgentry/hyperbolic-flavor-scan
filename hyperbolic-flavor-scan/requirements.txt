# Core dependencies for hyperbolic-flavor-scan
# Install via: pip install -r requirements.txt --break-system-packages
# SnapPy must be installed separately: pip install snappy

snappy>=3.1
scipy>=1.11
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
